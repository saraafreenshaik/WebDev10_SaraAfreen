function calculateTriangleArea(base, height) {
    return 0.5 * base * height;
}

let base = 8;
let height = 5;
let areaTriangle = calculateTriangleArea(base, height);
console.log("Area of the Triangle:", areaTriangle);
